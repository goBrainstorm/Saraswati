# Server
PORT=8000

# Paths
DB_PATH=data/knowledge.db
CACHE_DIR=cache
INPUT_DIR=input

# Retention
LOCAL_RETENTION_DAYS=7

# Schedule (cron)
SCHEDULE_CRON=0 3 * * *

# Nextcloud (WebDAV)
NEXTCLOUD_URL=
NEXTCLOUD_USER=
NEXTCLOUD_PASS=
NEXTCLOUD_REMOTE_DIR=/Knowledge/Audio

# LLM (Phase 2)
LLAMA_SERVER_URL=http://localhost:11434
LLAMA_MODEL=gemma4:e4b

# Whisper (Phase 2)
WHISPER_MODEL=small
WHISPER_BATCH_SIZE=8
DENOISE_MAX_MB=100

# Qdrant (Phase 3)
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION=knowledge

# Tailscale — bind address for the uvicorn server
# Dev: 127.0.0.1 (localhost only)
# Production: set to your Tailscale IP (e.g. 100.x.x.x) so the server is
#   reachable from other Tailscale nodes without exposing a public port.
TAILSCALE_HOST=127.0.0.1
